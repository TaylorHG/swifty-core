{
  "name": "@swift-developer/swifty-core",
  "version": "0.0.10",
  "description": "Core of Swifty.js, a modular, scalable, and light-weight framework for Node.js APIs!",
  "main": "./distribution/core.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel core --out-dir distribution -q",
    "prepublish": "npm run build",
    "develop": "babel-watch app/develop.js",
    "debug": "npm run build && babel app --out-dir dist -q && cp app/app-config.json dist && node --inspect debug.js"
  },
  "repository": {
    "type": "git",
    "url": "https://git-codecommit.us-west-2.amazonaws.com/v1/repos/swifty-core"
  },
  "author": "Taylor Brooks",
  "license": "MIT",
  "devDependencies": {
    "@swift-developer/swifty-rest": "0.0.10",
    "@swift-developer/swifty-watcher": "0.1.0",
    "babel-cli": "^6.18.0",
    "babel-eslint": "^7.1.1",
    "eslint": "^3.13.0"
  },
  "dependencies": {
    "@swift-developer/swifty-objects": "0.0.1",
    "colors": "^1.1.2",
    "babel-core": "^6.21.0",
    "babel-preset-swifty": "0.0.7",
    "compression": "^1.6.2",
    "connect": "^3.5.0",
    "express": "^4.14.0"
  }
}
